# Home Git Repository

This repository is initialized at $HOME. By default, the .gitignore ignores everything to avoid accidentally committing personal files. To track specific files or directories, add unignore rules (lines starting with '!') to .gitignore.

Examples you can add to .gitignore to start tracking:
- !.zshrc
- !Projects/your-project/**
- !bin/**

